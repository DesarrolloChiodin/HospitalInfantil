<UserControl x:Class="ControlDePacientes.UserControls.BasicButtons"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Loaded="UserControl_Loaded">
    <Grid >
        <WrapPanel HorizontalAlignment="Center" Name="wrpButton">
            <Button  Name="btnGuardar" Style="{StaticResource SaveButton}" Click="btnGuardar_Click" />
            <Button Name="btnCancelar" Style="{StaticResource CancelButton}" Click="btnCancelar_Click" />
            <Button Name="btnEliminar" Style="{StaticResource DeleteButton}" Click="btnEliminar_Click" />
        </WrapPanel>
    </Grid>
</UserControl>
